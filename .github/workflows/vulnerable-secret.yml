name: vulnerable-secret

on:
  issues:
    types: 
      - opened
  ##workflow_dispatch:
    ##inputs:
      ##title:
        ##required: true
        ##type: string

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      password: ${{ secrets.password }}
      email: ${{ github.event.issue.body }}
    steps:
      - name: echo PR title
        run: echo ::add-mask::$email
             echo ${{ github.event.issue.title }}
        ##run: echo ${{ inputs.title }}
